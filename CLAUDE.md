# このリポジトリでの作業ルール

## 外部への投稿（MEO・SNS・ブログなど）は必ず事前承認を得る

MEO Dashboard（Googleビジネスプロフィール）・Facebook・Instagram・ブログなど、
**外部に公開される投稿は、勝手に公開してはいけません。**

1. まず投稿文の下書きをチャットでユーザーに見せる
2. 可能なツール（MEO Dashboardなど）では「下書き保存」までにとどめ、公開はしない
3. ユーザーの「OK」をもらってから初めて公開（投稿）する

この承認フローは省略しないこと（2026-07-27 ユーザー指示）。

また、MEO・SNS投稿の文体は**来院を勧める書き方をしない**こと。
「ご相談ください」「受けてみませんか」等の誘導文言は使わず、
あくまでも専門家としての情報提供に徹する（2026-07-27 ユーザー指示）。

## 成果物は必ず `main` にまとめる

作業ブランチだけに成果物を置くと、別のパソコンでセッションを開いたときに見えなくなります。
**作業が終わったら、必ず `main` ブランチにも取り込んでください。**

手順：

```bash
git checkout main
git pull origin main
git merge <作業ブランチ名>
git push origin main
```

指示されたブランチ（`claude/...`）への push も従来どおり行ったうえで、
**加えて** `main` にもまとめる、という運用です。どちらか一方ではありません。

## 既定のブランチが `main` ではない点に注意

このリポジトリの GitHub 上の既定ブランチは、現時点では
`claude/common-test-2025-ethics-od5838` のままです（`main` ではありません）。

そのため、`main` を更新したら**既定ブランチも同じ内容まで早送り**してください。
これをしないと、リポジトリのトップページに最新の成果物が出ません。

```bash
git push origin main:claude/common-test-2025-ethics-od5838
```

早送り（fast-forward）にならない場合は、強制 push はせずユーザーに相談してください。

## README.md（目次）を最新に保つ

`README.md` はこのリポジトリの成果物カタログです。
**新しい成果物を追加したら、README.md の該当セクションに1行追記してください。**
分類は「歯科医院」「情報発信・集客」「学習教材」「人間科学研究所」です。
どれにも当てはまらない場合は新しい見出しを作って構いません。

## 作業を再開するとき

ユーザーが「◯◯の続き」と言った場合、まず `README.md` と既存フォルダを見て
該当ファイルを特定してから作業を始めてください。過去の作業ブランチ（`claude/...`）も
履歴として残してあるので、必要なら参照できます。

## ユーザーとのやり取り

- 応答は**日本語**で行ってください。
- ユーザーは Git や GitHub の操作に詳しくありません。GitHub の画面操作をお願いするときは、
  **直リンク・クリックする場所・押すボタン名**まで具体的に案内してください。
- コマンド操作で代替できる場合は、画面操作をお願いする前にそちらを提案してください。
